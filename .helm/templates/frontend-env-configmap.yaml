apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-nginx-env-config
  labels:
    app: tenis-frontend
data:
  env-config.js: |-
    window._env_ = {
      API_SERVER: "api.{{ .Values.env_url }}",
      BACKEND_PORT: "443"
    };
